# Publication manifest

Included:

- exact accepted compute-node and relay boot/application images;
- firmware source and generated structural headers;
- fixed prompt/decode contract and six-board topology;
- pinned checkpoint identity and derived-weight manifests;
- host oracle, raw physical receipts, summary, and decoded report;
- reconstruction, deployment-planning, capture, reporting, and verification tools;
- fail-closed tests and `SHA256SUMS`.

Excluded:

- gated Meta checkpoint and exported layer/vocabulary weight bytes;
- compiler caches, ELF files, map files, and merged 16 MB convenience images;
- incomplete captures, one-prompt development gates, and superseded reports;
- failed or exploratory interleave/context experiments;
- firmware recovery dumps, NVS, credentials, tokens, and machine-specific caches;
- COM3 and unrelated SSOS-ESP32 working-tree changes.
